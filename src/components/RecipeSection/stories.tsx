import { Story, Meta } from '@storybook/react'
import RecipeSection from '.'

export default {
  title: 'RecipeSection',
  component: RecipeSection
} as Meta

export const Default: Story = () => <RecipeSection />
