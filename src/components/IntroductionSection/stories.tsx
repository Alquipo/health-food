import { Story, Meta } from '@storybook/react'
import IntroductionSection from '.'

export default {
  title: 'IntroductionSection',
  component: IntroductionSection
} as Meta

export const Default: Story = () => <IntroductionSection />
