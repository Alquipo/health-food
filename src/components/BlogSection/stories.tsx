import { Story, Meta } from '@storybook/react'
import BlogSection from '.'

export default {
  title: 'BlogSection',
  component: BlogSection
} as Meta

export const Default: Story = () => <BlogSection />
